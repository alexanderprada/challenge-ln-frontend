export interface CardData {
  title: string;
  linkToDetail: string;
  imageUrl: string;
  lead: string;
  authorImageUrl?: string;
  description?: string;
  authorName?: string;
  readTime?: number;
  badge?: string;
}
